<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StyleButtons" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
       <item name="android:background">#3333cc</item>
        <item name="android:layout_width">250dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">#FFFF</item>
        <item name="android:layout_margin">10dp</item>

    </style>
    <style name="StyleLocationsListview" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:textColor">#3333cc</item>
        <item name="android:textStyle">bold</item>

    </style>

</resources>
